# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Legacy data (no longer used)
*.duckdb
*.duckdb.wal
*.db
fpl_league_data_*.json

# Node (browser testing artifacts)
node_modules/
package.json
package-lock.json

# Verification screenshots
verification_*/

# Environment
.env
*-service-account*.json

# Firebase
firebase-debug.log
firebase-debug.*.log
.firebase/
functions/venv/
functions/fpl_predictor/
# Netlify
.netlify/
dist/

# Secrets — NEVER commit
secrets.json
*service-account*.json

# Personal / unrelated files
*.exe
*.gb1
*.gm81
לב*

# ML training artefacts
ml_venv/
ml/

# Claude AI session files (local only) — but track shared skills so all agents/checkouts get them
.claude/*
!.claude/skills/
# (settings.local.json, launch.json, etc. stay ignored)

# Python cache
__pycache__/

# Scratch folder (temporary/local work)
scratch/



# Local DB backups (contain user emails) — never commit
dr_backup/
draft_result_backup_*.json
firestore_export_*.json

# Operational artifacts accidentally committed via #238 — never track these.
# Local backups / rollback snapshots / migration dumps:
*_BACKUP*.json
squads_BACKUP*.json
wishlist_bids_snapshot*.json
wishlist_RESULTS_*.json
wishlist_claim_txns_*.json
DELETED_rollback_*.json
PREMIGRATE_*.json
*_to_reverse.json
backups/
# Generated / exported reports + one-off data dir:
fifa_fantasy_squads/
fifa_live_sync.py
wc_draft_analysis.html
wc_non_squad_players.html
*.pages
WC2026_GOLIVE_HANDOFF.md
